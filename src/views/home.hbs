{{!-- Khối style này bạn có thể giữ ở đây hoặc chuyển sang file style.css trong public/css --}}
<style>
    /* Hiệu ứng nổi lên khi hover vào thẻ sản phẩm */
    .product-card {
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        border: 1px solid #eee !important;
    }
    .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.08) !important;
        border-color: var(--primary-sky) !important;
    }

    /* Giới hạn tên sản phẩm hiển thị tối đa 2 dòng, dài quá sẽ có dấu ... */
    .product-name {
        font-size: 14px;
        line-height: 1.4;
        height: 39px; /* Khoảng 2 dòng */
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    /* Định dạng giá tiền (Dùng màu cam khớp với số lượng giỏ hàng của bạn) */
    .product-price {
        color: #f97316; 
        font-size: 16px;
    }

    /* Đảm bảo ảnh sản phẩm luôn vuông vắn không bị méo */
    .product-img-wrapper {
        aspect-ratio: 1 / 1;
        overflow: hidden;
        padding: 10px;
    }
    .product-img-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: contain;
    }
</style>

<div class="container-fluid p-0">
    {{!-- Section: BANNER HERO --}}
    <div class="banner-section rounded-3 mb-5 position-relative overflow-hidden d-flex align-items-center" 
         style="min-height: 450px; 
                background: linear-gradient(90deg, rgba(14,165,233,0.95) 0%, rgba(2,132,199,0.8) 100%), 
                            url('https://images.unsplash.com/photo-1626224583760-446f98ce2307?q=80&w=1920&auto=format&fit=crop') center/cover no-repeat;">
    
        <div class="container-fluid px-4 px-md-5 position-relative z-1">
            <div class="row">
                <div class="col-12 col-md-8 col-lg-6 text-white">
                    <span class="badge bg-warning text-dark px-3 py-2 rounded-pill fw-bold mb-3">🔥 BỘ SƯU TẬP 2026</span>
                    <h1 class="display-4 fw-bold mb-3" style="line-height: 1.2;">Bứt Phá Tốc Độ,<br>Làm Chủ Sân Đấu!</h1>
                    <p class="lead mb-4" style="opacity: 0.9;">Trang bị vợt cầu lông chính hãng từ Yonex, Victor, Lining với mức giá tốt nhất thị trường. Tặng kèm cước và quấn cán cho mọi đơn hàng.</p>
                    <div class="d-flex gap-3">
                        <a href="/products" class="btn btn-warning btn-lg fw-bold px-4 rounded-pill shadow-sm">XEM NGAY</a>
                        <a href="/sale" class="btn btn-outline-light btn-lg fw-bold px-4 rounded-pill">🎁 ƯU ĐÃI THÁNG NÀY</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{!-- Section: SẢN PHẨM MỚI (TABS) --}}
    <div class="mb-5">
        
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center border-bottom pb-2 mb-4">
            <h3 class="fw-bold m-0 text-uppercase mb-3 mb-md-0" style="color: var(--text-dark);">
                <i class="bi bi-stars text-sky me-2"></i>Sản phẩm mới
            </h3>
            
            <ul class="nav nav-pills gap-2" id="newProductsTab" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-vot" type="button" role="tab">Vợt</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-giay" type="button" role="tab">Giày</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-ao" type="button" role="tab">Áo</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-quan" type="button" role="tab">Quần</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-balo" type="button" role="tab">Balo</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link rounded-pill px-4 fw-bold" data-bs-toggle="pill" data-bs-target="#tab-phu-kien" type="button" role="tab">Phụ Kiện</button>
                </li>
            </ul>
        </div>

        <div class="tab-content" id="newProductsTabContent">
            
            <div class="tab-pane fade show active" id="tab-vot" role="tabpanel">
                <div class="row g-3">
                    {{#each newestVot}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <div class="tab-pane fade" id="tab-giay" role="tabpanel">
                <div class="row g-3">
                    {{#each newestGiay}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <div class="tab-pane fade" id="tab-ao" role="tabpanel">
                <div class="row g-3">
                    {{#each newestAo}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <div class="tab-pane fade" id="tab-quan" role="tabpanel">
                <div class="row g-3">
                    {{#each newestQuan}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <div class="tab-pane fade" id="tab-balo" role="tabpanel">
                <div class="row g-3">
                    {{#each newestBalo}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <div class="tab-pane fade" id="tab-phu-kien" role="tabpanel">
                <div class="row g-3">
                    {{#each newestPhuKien}}
                    <div class="col-6 col-md-4 col-lg-3">
                        <div class="card h-100 product-card rounded-3">
                            <a href="/product/{{this.slug}}" class="product-img-wrapper text-center">
                                <img src="{{this.imageUrl}}" class="card-img-top" alt="{{this.name}}">
                            </a>
                            <div class="card-body d-flex flex-column text-center p-3">
                                <a href="/product/{{this.slug}}" class="text-decoration-none text-dark mb-2">
                                    <h5 class="card-title product-name fw-bold m-0" title="{{this.name}}">{{this.name}}</h5>
                                </a>
                                <p class="card-text product-price fw-bold mt-auto mb-0">{{this.formattedPrice}}</p>
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

        </div>
        
    </div>
</div>